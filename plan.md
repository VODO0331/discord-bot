# **Discord 市場與產業資訊自動推播系統實作計畫 (plan.md)**

# **1\. 系統架構與檔案結構 (Repository Layout)**

專案採用標準 Python 模組化結構，職責分離清晰，各模組均支援單元測試與 `--dry-run` 本機調試：.

├── .github/

│   └── workflows/

│       ├── alert.yml               \# 每 30 分鐘：突發快訊掃描

│       ├── morning\_brief.yml       \# 週一至週五 08:30：開盤晨報 (台股開盤前瞻)

│       ├── market\_wrap.yml         \# 週一至週五 17:30：盤後綜述 (台股收盤+美股前瞻)

│       └── weekly\_deepdive.yml     \# 週六 10:00：週末產業週報 (當週回溯綜述)

├── config.yaml                     \# 核心觀察標的、RSS 清單、關鍵字宣告式設定

├── requirements.txt                \# 專案依賴庫

├── main.py                         \# CLI 入口點 (支援 \--mode 與 \--dry-run)

├── src/

│   ├── \_\_init\_\_.py

│   ├── config.py                   \# 讀取 config.yaml 與 GitHub Secrets 環境變數

│   ├── db.py                       \# Supabase 連線、URL 去重比對、新增文章、30 天過期清理

│   ├── data\_sources/

│   │   ├── \_\_init\_\_.py

│   │   ├── market.py               \# yfinance (美股指數、個股、TSM ADR) \+ 台股行情

│   │   ├── rss.py                  \# feedparser RSS 抓取與正規化

│   │   └── extractor.py            \# trafilatura 內文提取 (含 RSS description 降級容錯)

│   ├── pipeline/

│   │   ├── \_\_init\_\_.py

│   │   ├── filter.py               \# 第一階段：規則過濾 (實體標的 \+ 事件關鍵字)

│   │   ├── summarizer.py           \# 第二階段：Gemini Flash 摘要與評分 (1\~5 分)

│   │   └── formatter.py            \# Discord Embed 訊息卡片排版建構器

│   └── delivery/

│       ├── \_\_init\_\_.py

│       └── webhook.py              \# Discord Webhook 派發器 (狀態碼 204 驗證、分流派發)

└── scripts/

    └── supabase\_init.sql           \# Supabase PostgreSQL 初始化 DDL 腳本

# **2\. 模組實作規格與技術細節**

## **2.1 資料庫模組 (`scripts/supabase_init.sql` & `src/db.py`)**

* **資料表 DDL**：

CREATE TABLE IF NOT EXISTS processed\_articles (

    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,

    url\_hash VARCHAR(64) NOT NULL UNIQUE,

    title TEXT NOT NULL,

    source VARCHAR(64) NOT NULL,

    published\_at TIMESTAMPTZ,

    category VARCHAR(32) DEFAULT 'general',

    summary\_markdown TEXT,

    created\_at TIMESTAMPTZ DEFAULT NOW()

);

CREATE INDEX IF NOT EXISTS idx\_processed\_articles\_hash ON processed\_articles(url\_hash);

CREATE INDEX IF NOT EXISTS idx\_processed\_articles\_created\_at ON processed\_articles(created\_at);

* **職責與方法**：  
  * `is_article_processed(url_hash: str) -> bool`：快速判定文章是否已推播過。  
  * `record_article(article_dict: dict)`：將推播成功之文章資訊存入資料庫。  
  * `prune_old_records(days: int = 30)`：定時清理 30 天以前的舊資料，維持輕量資料庫。

## **2.2 宣告式設定 (`config.yaml` & `src/config.py`)**

* **結構定義**：  
  * `watchlist.us_indices`：`["^SOX", "^IXIC", "^GSPC"]`  
  * `watchlist.us_stocks`：`["TSM", "NVDA", "AAPL", "MSFT"]`  
  * `watchlist.tw_stocks`：`["2330.TW", "2454.TW", "2317.TW"]`  
  * `rss_feeds`：國際與台灣優質新聞源（如科技新報、TechCrunch、中央社財經）。  
  * `alert_keywords`：實體標的（台積電、輝達、聯準會、OpenAI 等）與重大動態詞（併購、制裁、降息、暴跌等）。

## **2.3 資料來源模組 (`src/data_sources/`)**

* **市場數據 (`market.py`)**：  
  * 使用 `yfinance` 批次查詢指數與個股即時報價、前日收盤價、漲跌幅（%）。  
  * 晨報模式：額外查詢台積電 ADR (`TSM`) 漲跌幅度作為台股開盤前瞻。  
* **新聞與內文 (`rss.py` & `extractor.py`)**：  
  * `rss.py`：解析各 RSS Feed，將項目正規化為統一物件（title, link, published\_time, summary）。  
  * `extractor.py`：透過 `trafilatura.fetch_url` 與 `trafilatura.extract` 抓取新聞正文。若回傳為 None 或連線錯誤，自動 fallback 回傳 RSS 提供的原始簡介文字。

## **2.4 核心管線模組 (`src/pipeline/`)**

* **雙階段過濾 (`filter.py`)**：  
  * `stage_one_filter(article, config) -> bool`：檢查標題與內文是否同時包含「核心標的清單」與「重大事件動作詞」，未命中者直接略過，零 Token 消耗。  
* **AI 提煉 (`summarizer.py`)**：  
  * 採用 `google-genai` 調用 `gemini-1.5-flash`。  
  * 快訊模式：要求回傳 JSON，包含 `significance_score` (1\~5)、`core_event` (一句話)、`key_details` (2\~3點)、`industry_impact` (產業鏈影響)。  
  * 晨報/盤後模式：傳入整合後的行情數據與精選焦點新聞，生成結構化早盤/盤後重點。  
  * 週報模式：撈取 Supabase 過去 7 天記錄，總結本週 3 大產業技術趨勢。  
* **卡片排版 (`formatter.py`)**：  
  * 建構符合 Discord Webhook 規格之 Embed JSON 字典，注入色碼（多頭綠 `#2ECC71`、空頭紅 `#E74C3C`、總經藍 `#3498DB`）與標籤。

## **2.5 派發模組 (`src/delivery/webhook.py`)**

* 根據執行模式分流：  
  * 快訊 (`alert`) 派發至 `DISCORD_WEBHOOK_ALERT_URL`。  
  * 晨報/盤後/週報 (`morning` / `wrap` / `weekly`) 派發至 `DISCORD_WEBHOOK_DAILY_URL`。  
* 檢查 HTTP Response 狀態碼為 204，並實作指數退避重試（Exponential Backoff）避免網路瞬斷。

## **2.6 CLI 主入口與除錯 (`main.py`)**

* 接受參數：  
  * `--mode {alert,morning,wrap,weekly}`（必填）  
  * `--dry-run`（選填，跳過 Webhook POST 與 DB 寫入，改將生成的 Embed JSON 印出於終端機）

# **3\. 實作任務清單 (Step-by-Step Todo List)**

## **Phase 1: 專案基礎建設與資料庫設定**

- [x] 1.1 建立 GitHub Repository 並配置 `.gitignore`、`requirements.txt`。  
- [x] 1.2 撰寫 `scripts/supabase_init.sql`，提供 `processed_articles` 表與索引初始化 DDL。  
- [x] 1.3 撰寫 `config.yaml` 宣告式設定檔（定義觀察名單、RSS 與過濾詞庫）。  
- [x] 1.4 實作 `src/config.py`，載入 YAML 設定與 GitHub Secrets 環境變數。

## **Phase 2: 資料儲存與資料來源模組**

- [x] 2.1 實作 `src/db.py`：Supabase REST Client 初始化、URL SHA-256 雜湊比對與記錄寫入函式。  
- [x] 2.2 實作 `src/data_sources/market.py`：封裝 `yfinance` 行情擷取（美股四大指數、TSM ADR、科技股）。  
- [x] 2.3 實作 `src/data_sources/rss.py`：封裝 `feedparser` 批次掃描 RSS 並產出標準化資料結構。  
- [x] 2.4 實作 `src/data_sources/extractor.py`：整合 `trafilatura` 內文萃取與降級容錯邏輯。

## **Phase 3: 智慧過濾、AI 提煉與卡片排版**

- [x] 3.1 實作 `src/pipeline/filter.py`：雙階段過濾之第一階段正則/字串規則匹配。  
- [x] 3.2 實作 `src/pipeline/summarizer.py`：Gemini 1.5/2.5 Flash 結構化 Prompt 與輸出解析（快訊評分、晨報盤後綜述、週報提煉）。  
- [x] 3.3 實作 `src/pipeline/formatter.py`：組裝 Discord Embed 訊息卡片（含漲跌顏色、欄位排版、來源標籤）。

## **Phase 4: Webhook 派發與 CLI 整合**

- [x] 4.1 實作 `src/delivery/webhook.py`：雙頻道分流派發與 HTTP 狀態碼驗證。  
- [x] 4.2 實作 `main.py`：Argparse CLI 介面（支援 `--mode` 與 `--dry-run`）。  
- [x] 4.3 實作本機端除錯流程：針對 4 種模式進行 `--dry-run` 終端機格式與流程驗證（全部通過）。

## **Phase 5: GitHub Actions 雲端自動化與上線**

- [x] 5.1 於 GitHub 儲存庫設定 5 項 GitHub Secrets（提供 `.env.example` 樣板與欄位說明）。  
- [x] 5.2 建立 `.github/workflows/alert.yml`（每 30 分鐘執行一次）。  
- [x] 5.3 建立 `.github/workflows/morning_brief.yml`（週一至週五 08:30 執行）。  
- [x] 5.4 建立 `.github/workflows/market_wrap.yml`（週一至週五 17:30 執行）。  
- [x] 5.5 建立 `.github/workflows/weekly_deepdive.yml`（週六 10:00 執行）。  
- [x] 5.6 於 GitHub Actions 手動點擊 `workflow_dispatch` 進行雲端全鏈路驗收（代碼已成功推送至 https://github.com/VODO0331/discord-bot）。


